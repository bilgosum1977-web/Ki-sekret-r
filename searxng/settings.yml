use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  base_url: "https://ki-sekret-r.onrender.com"
  secret_key: "ein_sicheres_passwort_hier"

# Das schaltet die fehlerhafte IP-Prüfung ab:
enabled_plugins:
  - 'Limiter plugin'
  
# Deaktiviert den blockierenden Schutz komplett:
limiter:
  enabled: false
